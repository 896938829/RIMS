# RIMS 后端基础地址（含 /api/v1 前缀）
BASE_URL=http://localhost:8080/api/v1

# 默认登录账号（与 migrations/000001_init.sql 中的 seed admin 对齐）
ADMIN_USER=admin
ADMIN_PASSWORD=admin123

# 默认仓库 ID，作为 X-Warehouse-ID 头发送；留空则由后端回退到用户默认仓库
DEFAULT_WAREHOUSE_ID=1

# 日志目录（相对于当前工作目录）
LOG_DIR=./logs
